# ====================================
# DEPENDENCIAS DEL PROYECTO FLASK
# ====================================
# Instalar con: pip install -r requirements.txt

# FRAMEWORK PRINCIPAL
Flask==3.0.0                    # Framework web minimalista y flexible
Werkzeug==3.0.1                 # Librería WSGI (viene con Flask)

# EXTENSIONES ESENCIALES
Flask-CORS==4.0.0               # Manejo de Cross-Origin Resource Sharing
Flask-SQLAlchemy==3.1.1         # ORM para base de datos
python-dotenv==1.0.0            # Carga variables de entorno desde .env

# BASE DE DATOS
SQLAlchemy==2.0.23              # ORM potente y flexible

# UTILIDADES
python-dateutil==2.8.2          # Manejo avanzado de fechas

# VALIDACIÓN Y SERIALIZACIÓN (opcional pero recomendado)
marshmallow==3.20.1             # Serialización/deserialización de objetos
flask-marshmallow==0.15.0       # Integración de Marshmallow con Flask

# TESTING (desarrollo)
pytest==7.4.3                   # Framework de testing
pytest-flask==1.3.0             # Extensión de pytest para Flask
pytest-cov==4.1.0               # Cobertura de tests

# PRODUCCIÓN
gunicorn==21.2.0                # Servidor WSGI para producción
psycopg2-binary==2.9.9          # Driver PostgreSQL (para producción)

# SEGURIDAD (si necesitas autenticación JWT)
# PyJWT==2.8.0                  # Descomenta si usarás JWT
# Flask-JWT-Extended==4.5.3     # Descomenta si usarás JWT

# MIGRACIONES DE BASE DE DATOS (recomendado cuando empieces a usar BD)
# Flask-Migrate==4.0.5          # Descomenta cuando necesites migraciones
# alembic==1.13.0               # Descomenta cuando necesites migraciones
